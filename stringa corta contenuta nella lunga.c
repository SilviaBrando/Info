/*Scrivere un programma che date in input due stringhe di lunghezza diversa indichi se la più
corta è contenuta solo una volta nella più lunga.*/

#include <stdio.h>
#include <string.h>
#define N 100

int main(){
    int i,
    char s1[N], s2[N];

    lung1=strlen(s1);
    lung2=strlen(s2);

    printf("Inserire la prima stringa: ");
    scanf("%s", s1);

    printf("Inserire la seconda stringa: ");
    scanf("%s", s2);


}
